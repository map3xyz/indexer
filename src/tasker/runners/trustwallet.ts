import { TokenList } from '@uniswap/token-lists'

export async function ingestTwaForNetwork(network: string): Promise<TokenList> {
    throw new Error('Function not yet implemented');
    // const tokenlist = await getTokenlistFromTrustWallet(network);
    // return tokenlist;
}